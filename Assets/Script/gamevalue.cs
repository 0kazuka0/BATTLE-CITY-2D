using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gamevalue : MonoBehaviour
{
    public static int gettanklifeup = 3;
    public static bool gettimerfreeze = false;
    public static bool getgranadenuke = false;
    public static bool getshovelIwall = false;

    public static bool gameover = false;

    public static int score = 0;//ค่า คะเเนนรวม
    public static int scoreT = 0;//จำนวนศัตรู1
    public static int scoreFT = 0;//จำนวนศัตรู2
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
